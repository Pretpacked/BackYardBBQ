{% extends 'base.html.twig' %}

{% block title %}{% endblock %}

{% block body %}

    {{ form_start(form) }}
        {{ form_row(form.name) }}
        {{ form_row(form.email) }}
        {{ form_row(form.subject) }}
        {{ form_row(form.message) }}
        
        <button class="btn btn-lg btn-primary" type="submit">Upload barbecue</button>

    {{ form_end(form) }}

{% endblock %}
