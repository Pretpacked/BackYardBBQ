{% extends 'base.html.twig' %}

{% block title %}{% endblock %}

{% block body %}

    {{ form_start(form) }}
        {{ form_row(form.name) }}
        {{ form_row(form.barbecue_price) }}
        {{ form_row(form.image) }}
        {{ form_row(form.description) }}

        <button class="btn btn-lg btn-primary" type="submit">Upload barbecue</button>

    {{ form_end(form) }}

{% endblock %}
